import { Locale } from "@types";

export const localesOptions = [
  { localeCode: Locale.EN, localeName: "English" },
  { localeCode: Locale.IT, localeName: "Italian" },
];

export const preferencesEN = {
  locale: Locale.EN,
};

export const preferencesPL = {
  locale: Locale.IT,
};
