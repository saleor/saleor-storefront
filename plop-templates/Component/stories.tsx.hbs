import React from "react";

import { Story } from "@storybook/react";

import { {{pascalCase name}} } from ".";
import { I{{pascalCase name}}Props } from "./{{pascalCase name}}";

export default {
title: "@components/{{ componentType }}/{{pascalCase name}}",
component: {{pascalCase name}},
};

const Template: Story<I{{pascalCase name}}Props> = args => (
    <{{pascalCase name}} {...args} />
);

export const Empty = Template.bind({});
Empty.args = { };